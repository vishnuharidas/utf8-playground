function NotesPanel({ className }: { className: string }) {
    return (
        <div className={className}>
            <h2>Notes</h2>
            <p>This is the notes panel.</p>
        </div>
    );
}
export default NotesPanel;